// Central export for all type definitions

// Acta and Electoral types
export * from './acta.types';

// Organization types
export * from './organization.types';

// Legacy types (for backward compatibility with mock data)
export type { ElectoralData, PoliticalParty } from './electoralTypes';
